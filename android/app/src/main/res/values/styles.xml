<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Legacy base theme (kept as-is) -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- MAIN APP THEME -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <!-- IMPORTANT: do NOT make the window transparent -->
        <!-- removed: <item name="android:background">@null</item> -->

        <!-- Ensure content is laid out BELOW the status bar -->
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>

        <!-- Opaque status bar (pick your toolbar/background color) -->
        <item name="android:statusBarColor">@android:color/white</item>
        <!-- Dark status bar icons on light bg (flip to false if you use a dark bar) -->
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Splash; hand off to main theme on Android 12+ -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>

</resources>
